FROM node:20-slim

WORKDIR /app

COPY server/package.json server/package-lock.json ./
RUN npm ci --omit=dev 2>/dev/null || npm install

COPY server/ ./

# tsx is a devDependency; install it explicitly for running from source
RUN npm install tsx

EXPOSE 13002

CMD ["npx", "tsx", "src/index.ts"]
